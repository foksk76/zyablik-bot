# Contributing

Проект небольшой, поэтому процесс максимально простой.

## Перед изменениями

- прочитать `README.md`;
- прочитать `AGENTS.md`;
- проверить текущую задачу в `tasks/sprints/`;
- убедиться, что изменение не выходит за границы текущего этапа.

## Требования к изменениям

- один PR или коммит — одна понятная задача;
- реальные секреты и внутренние значения не добавлять;
- организационные названия не использовать;
- изменения в поведении webhook сопровождать обновлением документации;
- крупные архитектурные решения фиксировать через ADR.

## Проверка

Перед завершением работы выполнить:

```bash
npm test
```

Если тесты недоступны, проверить:

- нет секретов и реальных идентификаторов;
- README, INSTALL и docs не противоречат друг другу;
- основной webhook остается в `src/zabbix-media-type/max-webhook.js`;
- новые решения зафиксированы ADR, если они меняют архитектуру, процесс или границы.

## Лицензия

Проект лицензирован по [Apache License 2.0](LICENSE). Путём внесения вклада
Вы соглашаетесь на лицензирование ваших вкладов на условиях Apache-2.0.
