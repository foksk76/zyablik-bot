{
  "name": "zyablik-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Zyablik — bot for delivering notifications in MAX messenger",
  "license": "Apache-2.0",
  "scripts": {
    "test": "node --require ./tests/setup.js --test",
    "verify": "npm test",
    "build:ui": "cd src/queue-monitor/ui && npm install && npm run build",
    "build": "npm run build:ui"
  },
  "dependencies": {
    "@okta/jwt-verifier": "^4.0.2",
    "better-sqlite3": "^11.10.0"
  },
  "engines": {
    "node": ">=20"
  }
}
