# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime / local env
.env
.env.*
!.env.example

# Runtime databases
delivery-queue.db*

# Runtime config on the stand (ADR-0045): активный файл + служебные
# (.lkg/.pending/.staged/.bad.json). $VAR-ссылки, но это стендовый артефакт.
config/
config/**
!config/.gitkeep

# Node / tooling
node_modules/
dist/
build/
coverage/

# Queue-monitor UI build artifacts (ADR-0034)
src/queue-monitor/ui/node_modules/
src/queue-monitor/ui/dist/

# OS / editors
.DS_Store
.idea/
.vscode/
.zcode/

# Local AI / Codex skills installs
.agents/external-skills/
.agent-skills/
.codex/skills/
.local/agent-skills/
vendor/agent-skills/

# Session memory (не для репозитория)
docs/memory.md

# Local tooling artifacts (opencode, playwright MCP)
.opencode/
.playwright-mcp/

# Secrets and local exports
*token*
*secret*
!src/queue-monitor/ui/src/tokens/
!docs/decisions/ADR-0013-safe-logger-secret-redaction.md
*.bak
*.tmp
src/queue-monitor/ui/storybook-static/
